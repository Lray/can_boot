#ifndef CAN_DRIVER_STM32_H
#define CAN_DRIVER_STM32_H

/*
 * Private module boundary for the STM32 FDCAN implementation.
 *
 * The public CAN contract remains in can_driver.h.  This header exists so
 * the implementation has an explicit module pair without exposing HAL types
 * to transport callers.
 */

#endif /* CAN_DRIVER_STM32_H */
