#!/bin/sh
set -efu

endpoint=/run/ecu-ota/remote-handler/ecu-v1
attempt=0

while [ "$attempt" -lt 30 ]; do
    [ -S "$endpoint" ] && exit 0
    attempt=$((attempt + 1))
    sleep 1
done

printf '%s\n' 'ecu-ota: Remote Handler endpoint did not become ready' >&2
exit 1
